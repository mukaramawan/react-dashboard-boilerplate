import { motion as Motion } from 'framer-motion';
import { Bell, Lock, Palette, Globe, Moon, Sun } from 'lucide-react';

const Settings = () => {
  return (
    <div className="p-8">
        <h2 className="text-2xl font-bold mb-6">Settings</h2>
    </div>
  );
};

export default Settings;
